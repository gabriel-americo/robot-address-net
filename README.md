Para criar a tabela de banco de dados.
CREATE TABLE "Enderecos" ( "Id" serial, "CEP" VARCHAR(255), "Logradouro" VARCHAR(255), "Bairro" VARCHAR(255), "UF" VARCHAR(255), "Status" int, "Robo" VARCHAR(255) );
